AWSTemplateFormatVersion: '2010-09-09'
Description: Creates an SNS tpic

Resources:
  TodoUpdates:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: "stratospheric-todo-updates"

Outputs:
  TodoUpdatesARN:
    Value: !Ref TodoUpdates
