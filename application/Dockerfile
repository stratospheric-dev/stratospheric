FROM  openjdk:11.0.7-jdk

ARG TODO_COGNITO_USER_POOL_ID
ARG TODO_COGNITO_CLIENT_ID
ARG TODO_COGNITO_CLIENT_SECRET

ARG JAR_FILE=build/libs/*.jar
COPY ${JAR_FILE} app.jar

ENTRYPOINT ["java", "-jar", "/app.jar"]
