FROM  openjdk:11.0.7-jdk

ENV TODO_COGNITO_USER_POOL_ID=${TODO_COGNITO_USER_POOL_ID}
ENV TODO_COGNITO_CLIENT_ID=${TODO_COGNITO_CLIENT_ID}
ENV TODO_COGNITO_CLIENT_SECRET=${TODO_COGNITO_CLIENT_SECRET}

ARG JAR_FILE=build/libs/*.jar
COPY ${JAR_FILE} app.jar

ENTRYPOINT ["java", "-jar", "/app.jar"]
