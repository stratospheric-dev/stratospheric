cloud:
  aws:
    stack:
      auto: false
    region:
      static: eu-central-1
    instance:
      data:
        enabled: false
    sns:
      enabled: false

server:
  forward-headers-strategy: framework
